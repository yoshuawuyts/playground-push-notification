{
  "name": "playground-push-notification",
  "description": "try out push notifications",
  "repository": "yoshuawuyts/playground-push-notification",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "bankai start client",
    "test": "standard && npm run deps"
  },
  "dependencies": {},
  "devDependencies": {
    "bankai": "^8.0.0",
    "dependency-check": "^2.9.1",
    "standard": "^10.0.2",
    "tape": "^4.7.0"
  }
}
